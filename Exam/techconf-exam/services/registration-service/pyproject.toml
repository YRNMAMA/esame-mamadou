[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "registration-service"
version = "0.1.0"
description = "TechConf Registration Service"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0,<4",
    "requests>=2.31,<3",
    "techconf-common @ file:///Users/mamadougningue/EsameML/esame-mamadou/Exam/techconf-exam/services/common",
    "user-service @ file:///Users/mamadougningue/EsameML/esame-mamadou/Exam/techconf-exam/services/user-service",
    "event-service @ file:///Users/mamadougningue/EsameML/esame-mamadou/Exam/techconf-exam/services/event-service",
]

[project.optional-dependencies]
test = [
    "pytest>=7.4,<9",
    "pytest-cov>=4.1,<5",
    "responses>=0.23,<1",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["registration_service*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["registration_service"]
omit = [
    "registration_service/__main__.py",
    "registration_service/repository.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]